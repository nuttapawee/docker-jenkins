pipeline {    
stage('Build') {
    steps {
      echo "Running tests in a fully containerized environment..."
      dir ('/home/oracle/test-scripts') {
        sh './test.sh'
        sh docker logs -f elasticsearch
        sh docker logs -f kibana
      }
    }
  }
}
